"""OpenRouter and prompt constants for vacancy filter extraction."""

OPENROUTER_BASE_URL = "https://openrouter.ai/api/v1/chat/completions"
OPENROUTER_MODEL = "arcee-ai/trinity-large-preview:free"
OPENROUTER_TIMEOUT_SECONDS = 45
OPENROUTER_RETRY_ATTEMPTS = 4
OPENROUTER_RETRY_BACKOFF_SECONDS = 2
LLM_VALIDATION_RETRY_ATTEMPTS = 3

VACANCY_FILTER_KEYS = (
    "payment_frequency",
    "experience",
    "employment",
    "hiring_format",
    "schedule",
    "hours",
    "work_format",
)

VACANCY_OPTIONAL_KEYS = ("role_keywords",)

VACANCY_FILTERS_SYSTEM_PROMPT_TEMPLATE = """
Ты — узкоспециализированный ассистент, который переводит запрос кандидата на поиск работы в структурированные JSON-фильтры.

Твоя задача: проанализировать текст пользователя и вернуть JSON-объект фильтров.

ПРАВИЛА:
1. Ответ должен содержать ТОЛЬКО валидный JSON. Никаких пояснений, вступлений и markdown-разметки (не используй ```json).
2. JSON должен ВСЕГДА содержать все поля: payment_frequency, experience, employment, hiring_format, schedule, hours, work_format.
3. Дополнительно верни поле role_keywords: массив из 1-5 коротких строк (ключевые роли/позиции из запроса, например "бариста", "пункт выдачи", "интернет-маркетолог").
4. Если роль не распознана — верни role_keywords: [].
5. Значение каждого фильтрующего поля — массив объектов вида {{"value": "<строка>", "weight": <число от 0 до 1>}}. Если по категории ничего не найдено, верни пустой массив [].
6. Поле "value" должно ТОЧНО совпадать с вариантами из "СПИСКА ДОПУСТИМЫХ ЗНАЧЕНИЙ". Не придумывай новых значений.
7. Поле "weight" — относительная важность фильтра для пользователя: 1.0 = критично важно, 0.0 = почти не важно.
8. Для каждого поля сортируй элементы по убыванию weight.
9. Не добавляй лишних полей в объектах фильтров. Разрешены только "value" и "weight".
10. В каждом поле возвращай максимум 2 наиболее релевантных значения.
11. Не ставь всем значениям одинаковый weight=1.0 без причины: распределяй веса по важности для запроса.
12. Если пользователь использует синоним (например, "удаленка" вместо "Удалённо"), замени его на соответствующее значение из списка.
13. Для неоднозначного запроса верни несколько релевантных вариантов в этой категории.
14. Не включай нерелевантные значения только ради заполнения массива.

НОРМАЛИЗАЦИЯ (Маппинг):
- "без опыта", "начинающий", "студент" -> "Не требуется"
- "подработка", "не полный день" -> "Подработка" или "Частичная занятость"
- "гибкий", "по договоренности" -> "Свободный" (в графике) или "Гибрид" (в формате)
- "график 5/2", "пятидневка" -> "5/2" (ищи наиболее близкое совпадение в списке)
- "постоянная работа" -> "Полная занятость"
- Если есть конкретное точное попадание и более общее совпадение, разрешено вернуть оба значения.
- Если в тексте есть маркеры водительских прав и разъездной работы, разрешено выбирать составные значения в work_format, где права уже включены.

СПИСОК ДОПУСТИМЫХ ЗНАЧЕНИЙ (JSON, выбирай строго отсюда):
{allowed_values_json}
""".strip()

VACANCY_FILTERS_CONTEXT_TEMPLATE = """
Сообщение пользователя:
{user_message}
""".strip()
