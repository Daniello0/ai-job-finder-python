---
description: Design Document: JobFinder AI
globs: **/*.py,**/*.sql
alwaysApply: false
---

## 1. Introduction

**Task:** Optimization of job search for university graduates in the Republic of Belarus using neural networks.

**Main Idea:** 
Transitioning from syntactic search (keyword-based) to semantic search (meaning-based). The system combines vector embeddings with LLM-based query structuring to map graduate intent to relevant employer requirements.

**Tech Stack:**
- **Language:** Python 3.10+
- **Frontend:** Streamlit
- **Backend API:** FastAPI
- **ORM:** SQLAlchemy (with `asyncpg` driver)
- **Database:** PostgreSQL + pgvector
- **Neural Core:** Sentence-Transformers (`paraphrase-multilingual-MiniLM-L12-v2` model)
- **LLM:** Groq Llama 3.3 70B Versatile (`llama-3.3-70b-versatile`) via Groq OpenAI-compatible Chat Completions API

---

## 2. User Stories
- **Smart Profile:** As a graduate, I want to describe my skills, knowledge, and projects so that the system can find vacancies where this experience will be most useful.
- **Relevant Matching:** As a user, I want vacancies to be filtered and ranked according to my constraints (format, schedule, experience), so that irrelevant options are excluded early.
- **Top-5 Results:** As a user, I want to receive a list of up to 5 most suitable vacancies, ordered by relevance, so that I can quickly focus on the best opportunities.

---

## 3. Functional Requirements
- **Automated Data Collection:** Parsing vacancies from rabota.by with deep filtering for Junior/Intern positions.
- **Semantic Vectorization:** Converting descriptions into vectors of dimension 384 (a mathematical representation of text meaning in 384-dimensional space for precise comparison).
- **LLM Filter Extraction:** Converting free-form user intent into structured weighted filters and role keywords.
- **Adaptive Filter Relaxation:** Automatically dropping the least important filters when too few candidates are found.
- **Vector Search:** Utilizing cosine similarity to find relevant vacancies in the database.
- **Data Validation:** Strict typing of incoming requests using Pydantic models.

---

## 4. API Design (FastAPI)

Interaction between the interface and server logic is performed via a REST API:

- **GET `/api/v1/search`** — Takes a user query (and optional `limit`), runs LLM-based filter extraction, applies semantic retrieval, and returns up to 5 full vacancy objects with cosine distance.

---

## 5. Entity Model (PostgreSQL)

**Table `vacancies`**:
- `id`: Integer (PK).
- `title`: String (Job title).
- `company`: String (Company name).
- `salary`: String (Salary amount).
- `payment_frequency`: String (Payment frequency).
- `experience`: String (Required experience).
- `employment`: String (Employment type).
- `hiring_format`: String (Hiring format).
- `schedule`: String (Work schedule).
- `hours`: String (Working hours).
- `work_format`: String (Work format: office/remote).
- `skills`: Text (List of key skills).
- `url`: String (Link to the vacancy).
- `description`: Text (Full description).
- **`embedding`**: **Vector(384)** — Field for semantic search.

---

## 6. Technical Implementation (Internal Processes)

### Step A: Data Enrichment Process (Ingestion)
1. **Parser:** A scraper script populates the database with vacancies, leaving the vector field empty.
2. **Embedder:** A background task selects records without vectors, creates a textual representation of the vacancy based on the title, skills, and description, then generates a vector via the Sentence-BERT neural network and saves it to the database.

### Step B: Query Processing (Inference)
1. **User Input:** The user enters a text description of their experience in the Streamlit interface.
2. **FastAPI Request:** The client side sends this text to the backend for processing.
3. **LLM Structuring:** The backend sends the raw query to Groq LLM to infer weighted categorical filters and role keywords.
   - The request uses strict structured output via `response_format: json_schema`.
   - The schema is generated dynamically from current DB categorical values (`allowed_values`) for each filter key.
   - No retry mechanism is used for invalid JSON/validation mismatch: if the LLM response is invalid, the backend raises an error immediately.
4. **Filter Relaxation:** If strict filtering returns too few candidates, the backend removes the least important filter groups until the candidate set becomes sufficient.
5. **Query Vectorization:** The backend transforms the user's text into a numerical vector of the same dimension (384) as the vacancies in the database.
6. **Similarity Search + Re-ranking:** PostgreSQL/pgvector returns nearest vacancies by cosine distance; an additional domain-keyword boost improves practical relevance.
7. **Response:** The final result, including a concise assistant message and vacancy cards with links/details, is displayed in the interface.

---

## 7. Technical Nuances and Optimization
- **SQLAlchemy + pgvector:** Using a specialized extension for PostgreSQL ensures efficient handling of vector data types and high search speeds.
- **Prompt Engineering + Schema Enforcement:** The LLM system prompt is focused on strict JSON filter extraction, and API-level JSON Schema enforcement guarantees field-level structure and value constraints.
- **Process Synchronization:** The database vectorization process is decoupled from the user search process, preventing interface delays.
- **Interface:** Streamlit provides reactive data display and maintains the state of the current search session.